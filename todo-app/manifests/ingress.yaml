apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: meqdad-dtodo-ingress
spec:
  rules:
  - http:
      paths:
      - path: /
        pathType: Prefix
        backend:
          service:
            name: dtodo-svc
            port:
              number: 2345